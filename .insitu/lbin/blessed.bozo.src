#!/source/this/sh
main () {
    local p=${BOZO_BIN}/src.bozo
    local cmd=$1
    shift
    [ "${cmd}" == "" ] && { . ${p}.__null__ $* ; return $?; }
    [ -f ${p}.${cmd} ] && { . ${p}.${cmd}   $* ; return $?; }
    [ -f ${p}.${cmd} ] || { . ${p}.__bad__  $* ; return $?; }
}
main $*
